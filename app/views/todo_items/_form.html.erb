<%= form_for([@todo_list, @todo_list.todo_items.build]) do |f| %>
    <% if @todo_list.user == current_user %>
        <%= f.text_field :content, placeholder: "Nova Tarefa" %>
        <%= f.submit  "Criar" %>
    <% end %>
<% end %>