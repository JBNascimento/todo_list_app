<h4 style= "margin-top:40px;">Editar usuário: <%= current_user.email %></h4>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>
  <div class="row">
  <div class="col-sm-8">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "form-control" %>
  </div>
 
  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="col-sm-8">
    <%= f.label :senha %> <i>(Deixe em branco caso não mudar a senha)</i><br />
    <%= f.password_field :password, autocomplete: "new-password", class: "form-control" %>
    <% if @minimum_password_length %>
      <br />
      <em> Necessário ao menos <%=@minimum_password_length %> caracteres</em>
    <% end %>
  </div>

  <div class="col-sm-8">
    <%= f.label :confirme_sua_senha %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control" %>
  </div>

  <div class="col-sm-8">
    <%= f.label :senha_atual %> <i>(Necessário para efetuar as mudanças)</i><br />
    <%= f.password_field :current_password, autocomplete: "current-password", class: "form-control" %>
  </div>

  <div style="margin-top:20px;margin-bottom:60px;" class="col-sm-8">
    <%= f.submit "Atualizar", class: 'btn btn-primary' %>
  </div>
  </div>
<% end %>

<h3>Excluir a conta</h3>

<p>Caso deseje excluir a conta, clique no botão abaixo <%= button_to "Excluir Minha Conta", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete, class: 'btn btn-danger' %></p>

<%= link_to "Voltar", :back, :class => 'btn btn-primary' %>
